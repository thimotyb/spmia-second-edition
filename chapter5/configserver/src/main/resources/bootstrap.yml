spring:
  application:
    name: config-server
  profiles:
    active:
    - git
  cloud:
    config:
      server:
        bootstrap: true
        native:
          search-locations: classpath:/config
        git:
          uri: https://github.com/thimotyb/config-spmia-2.git
          searchPaths: licensingservice
          default-label: master  # Use this otherwise it will look for main branch
    
server:
  port: 8071
  
encrypt:
  key: fje83Ki8403Iod87dne7Yjsl3THueh48jfuO9j4U2hf64Lo 


#Enable all Spring Boot Actuator endpoints.
management:
  endpoints:
    web:
      exposure:
        include: "*"